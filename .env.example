# Server-only credential. Never prefix this with VITE_ or expose it in the browser.
HERMES_API_KEY=
HERMES_API_URL=http://127.0.0.1:8642
# Browser-safe Supabase Auth configuration. Never put a service_role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
